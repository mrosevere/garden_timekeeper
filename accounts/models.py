"""
Database models for the accounts app.

Includes optional profile extensions and user-specific settings such as
notification preferences. These models store additional information that
supplements Django's built-in User model.
"""

from django.db import models

# Create your models here.
